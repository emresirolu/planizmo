# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files — never commit real secrets
.env
.env.local
.env.production
.env*.local
.env*
# keep the documented template (negation MUST come after the .env* globs above,
# otherwise a later pattern would re-ignore it)
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
